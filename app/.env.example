# Supabase 连接配置 — 复制为 .env.local 并填入真实值(切勿提交真实密钥)
# 多机构 SaaS 后端。匿名公钥可暴露给前端;service_role 密钥仅用于服务端脚本,严禁进前端。
VITE_SUPABASE_URL=your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
# ⚠️ 不要在 .env 里填 LLM API key —— VITE_* 会被 vite 静态替换进 JS bundle,
# 部署到 GitHub Pages 等公开站点会被任何人通过 DevTools 抓取。
# LLM API key 由用户在 AI 助手面板里自行输入,只存浏览器 localStorage。
# 留空即可,本地开发默认走规则引擎,完整功能可用。
